# Processed_data

This folder contains intermediate `.RData` files generated during the analysis of brain metastasis (BrM) using the DSP platform. These files are used to speed up downstream analysis and figure generation by avoiding the need to reprocess raw inputs.
